import fortunes from fortunes;
import reactions from reactions;

// random reactie

const randomPositiveReaction = positiveReaction [Math.floor(Math.random() * positiveReaction.length)] // random 'het gaat goed'
const randomNegativeReaction = negativeReaction [Math.floor(Math.random() * negativeReaction.length)] // random 'het gaat slecht
const randomNeutralReaction = neutralReaction [Math.floor(Math.random() * neutralReaction.length)] // random 'het gaat whatever'

// random fortune

